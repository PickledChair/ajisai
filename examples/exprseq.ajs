func countup(until: i32) {
    countup_inner(0, until)
}

func countup_inner(x: i32, until: i32) {
    if x <= until {
        println_i32(x);
        countup_inner(x + 1, until)
    } else {
        ()
    }
}

countup(10);
